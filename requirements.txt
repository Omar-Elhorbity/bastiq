# Runtime dependencies (exact pins for reproducible builds).
# Core stack is pinned per handoff §3; the infra additions
# (psycopg, dj-database-url, gunicorn, redis, whitenoise) were explicitly approved.
Django==5.2.14
djangorestframework==3.16.1
djangorestframework-simplejwt==5.5.1
drf-spectacular==0.28.0
psycopg[binary]==3.3.4
dj-database-url==2.3.0
celery==5.6.3
redis==5.3.1
stripe==12.5.1
gunicorn==23.0.0
whitenoise==6.12.0
